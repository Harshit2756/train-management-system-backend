package com.tcs.train.util;

public class ScheduleValidator {
    public static boolean isValidSchedule(Object schedule) {
        // Dummy validation for now
        return schedule != null;
    }
} 